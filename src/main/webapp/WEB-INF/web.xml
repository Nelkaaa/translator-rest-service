<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

  <!-- Allow OPTIONS requests without authentication (for CORS preflight) -->
  <security-constraint>
    <web-resource-collection>
      <web-resource-name>CORS Preflight</web-resource-name>
      <url-pattern>/api/*</url-pattern>
      <http-method>OPTIONS</http-method>
    </web-resource-collection>
    <!-- No auth-constraint means OPTIONS is allowed without login -->
  </security-constraint>

  <!-- Protect all other methods -->
  <security-constraint>
    <web-resource-collection>
      <web-resource-name>Translator API</web-resource-name>
      <url-pattern>/api/*</url-pattern>
      <http-method>POST</http-method>
      <http-method>GET</http-method>
    </web-resource-collection>
    <auth-constraint>
      <role-name>user</role-name>
    </auth-constraint>
  </security-constraint>

  <login-config>
    <auth-method>BASIC</auth-method>
    <realm-name>ApplicationRealm</realm-name>
  </login-config>

  <security-role>
    <role-name>user</role-name>
  </security-role>

</web-app>
